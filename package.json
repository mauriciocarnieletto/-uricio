{
  "name": "uricio",
  "scripts": {
    "lerna": "lerna",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "dev": "lerna run dev",
    "clean": "rimraf **/dist/ && rimraf **/node_modules/ && node_modules && dist"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2"
  },
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
