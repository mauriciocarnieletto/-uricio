{
  "name": "uricio",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev --parallel --no-cache",
    "clean": "rimraf **/dist/ && rimraf **/node_modules/ && rimraf node_modules && rimraf dist && rimraf yarn.lock"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^27.5.0",
    "jest": "^28.1.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^28.0.2",
    "turbo": "latest"
  },
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "studies/*"
  ]
}
